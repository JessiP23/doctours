/**
 * Test stub for the `server-only` package, which throws when imported outside a
 * server component. Aliased in vitest.config.mts so server modules can be unit
 * tested in a plain node environment.
 */
export {};
